#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 2e5 + 10;
ll t[N],x[N];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    ll n,d,w;
    cin >> n >> d >> w;
    for(int i = 1;i <= n;++i)
    {
        cin >> t[i] >> x[i];
    }

}