#include <bits/stdc++.h>
using namespace std;
vector<string>s;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int n,m;
    cin >> n >> m;
    for(int i = 1;i <= n;++i)
    {
        string temp;
        cin >> temp;
        s.push_back(temp);
    }
}