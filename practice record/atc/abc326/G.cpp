#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 100;
ll a[N];
ll n,x,y;
// void dfs(ll step,ll direction,ll cur_x,ll cur_y)
// {

// }
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    cin >> n >> x >> y;
    for(int i = 1;i <= n;++i)
        cin >> a[i];
    
}