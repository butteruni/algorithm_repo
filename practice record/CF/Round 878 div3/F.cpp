#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
ll n,m,r;
vector<vector<int> >a;
struct shoot
{
    int t,d,crood;
}r[1010];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int t;
    cin >> t;
    while(t--)
    {
        cin >> n >> m;
        a.clear();
        a.resize(n + 10);
        for(int i = 0;i <= n;++i)
        {
            a[i].resize(m + 10);
        }
        cin >> r;
        for(int )
    }
}