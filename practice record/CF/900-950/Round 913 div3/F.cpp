#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
// ssssss
// psssss
// sssssp
// pssssp
void solve()
{
    int n;
    cin >> n;
    vector<int>a(n + 1);
    int ans = -1;
    for(int i = 1;i <= n;++i)
        cin >> a[i];
    
    int cnt = 0,pos = 0;
    for(int i = 1;i <= n;++i)
    {
        
    }


    
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int t;
    cin >> t;
    while(t--)
    {
        solve();
    }
}